# Database (used to store contact form submissions)
DB_HOST=localhost
DB_PORT=3306
DB_NAME=
DB_USER=
DB_PASS=
DB_CHARSET=utf8mb4

# SMTP (optional: email notification via PHPMailer)
SMTP_HOST=smtp.office365.com
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_ENCRYPTION=tls
SMTP_FROM=
SMTP_TO=

# Set to 1 temporarily to log SMTP errors to the PHP error log.
MAIL_DEBUG=0
